# Next task at the P05 handoff

The implementation/offline gate is complete at source commit `2fced2f7c06e6e8de29c88212cfcefc7cdec17cd`; `STATUS.md`, `TEST_MATRIX.md`, and `PROVIDER_MATRIX.md` contain the source/binary/test identities. Do not begin P06 or broaden the native transaction boundary as part of this handoff.

The next distinct task is one explicitly authorized live DeepSeek `deepseek-flash` run on a disposable selected-scene fixture. First record the exact profile/endpoint, model, scene path and context attachments, mode, finite request/tool/mutation/output/input/time limits, and a chosen monetary cap with rate source or explicit alternative limit mode. Configure the credential in the user-scoped store without pasting it into project files or logs; rotate the previously chat-shared key. Review the dry-run configuration and authorize that one run. Start with read-only inspection, then a nonmutating preview, then separately grant one native application. Observe usage, status, save/reopen, and any provider errors. No live call has yet been made.

If the native result is uncertain, reconcile the existing operation status before continuing. A new request ID or provider switch does not resolve it. Retain failed attempts and report application, persistence, and checking separately. A future task may add visible pending-request cancellation/profile-switch walkthrough and Save As dialog-cancellation coverage; they are explicitly `not_run` here.

## Historical P04 next task

The P03 save-failure gap is closed and the P04 code, offline tests, and programmed visible-editor fixture flow are complete. The next gate is a user-authorized **live P04 demo**, after the user chooses a real OpenAI model, confirms the exact selected-scene context, supplies a Credential Manager credential, and authorizes one finite run. No live request has yet been made. Do not silently infer a model, switch providers, use an API key merely because it exists, or begin P05 autonomous-building work.

Before that live run, manually exercise the new dock in the built editor, including provider status, Discuss, Propose, explicit Execute transition, review of the exact diff/revision, native grant, apply, status, save, and cancellation around a safe boundary. Use a copied trusted fixture. Record the actual selected model and configured limits, service/native event trace with secrets removed, counts and usage returned by the provider, and observed scene/save facts. If a native result is uncertain, inspect the existing operation status and reconcile it; never submit a new operation ID to replay it. Keep live verification `not_run` until this is performed.

Any future expansion of model-facing operations beyond `create_child`, 3D property/rename/remove support, or broader context requires its own focused tests and a new authorization scope. Retain the one-operation/one-scene transaction boundary.
