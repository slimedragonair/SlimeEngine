# P05 offline editor integration

Five profile-specific provider-format HTTP/SSE fixture runs traversed the production adapter code and the actual built editor/native authority path. The native test `tests/editor/slime_ai/test_slime_ai_p05_provider_wire.cpp` uses `tests/native_wire_fixture_service.ts` and checks one preview/grant/apply/status/undo/redo/dedup loop for each profile. The fixture also checks each initial and linked continuation request; see `P05_WIRE_CONFORMANCE.md` and `p05-native-wire-events.jsonl`. Result: 40/40 focused native cases, 507/507 assertions, exit 0 at source `2fced2f7c0` and binary SHA-256 `1E16C51ACB85D91DBB4EA4E71A098C9916EA4ACAA651ED291E79AD01A0B52855`.

The earlier visible GUI fake-provider walkthrough on a copied 2D scene checked the real dock input path: inspect selected scene, connect service, Propose preview with no edit, review for Execute, native grant/apply, undo/redo, save, close/reopen with exactly one marker. The P05 visible review then selected DeepSeek and model `deepseek-flash`; an unauthorized Start was blocked in the dock. Those GUI observations are recorded separately in `P04_CONTROL_REVIEW.md`. The five provider wire loops were automated built-editor tests, not five visible mouse-driven live demos.

Final fresh-copy 2D and 3D headless editor smokes both exited 0 (`p05-final-smoke-2d.log`, `p05-final-smoke-3d.log`). The prior first 2D baseline smoke exited with an access violation and its log is retained (`p05-baseline-smoke-2d-first.txt`); a repeat baseline run and the final committed-source run passed. No broad upstream editor suite or live provider request ran.
