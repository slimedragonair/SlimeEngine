# P05 production-adapter wire evidence

Run the built-editor native suite with `$env:SLIME_AI_TEST_WIRE_LOG='E:\SlimeEngine\docs\slime_ai\evidence\p05-native-wire-events.jsonl'; powershell -NoProfile -ExecutionPolicy Bypass -File tools/slime_ai/harness/run_native_tests.ps1`. Final result at source `2fced2f7c0`: 40/40 native cases, 507/507 assertions, exit 0; `native-tests.log` and `native-tests-manifest.txt`. The service `npm test` from `tools/slime_ai/agent_service` passed 63/63; `npm run typecheck` exited 0. Binary SHA-256 is `1E16C51ACB85D91DBB4EA4E71A098C9916EA4ACAA651ED291E79AD01A0B52855`.

`test_slime_ai_p05_provider_wire.cpp` invokes `tests/native_wire_fixture_service.ts` from the actual editor. The fixture feeds provider-format HTTP status, headers, and SSE bytes into the production OpenAI, Anthropic, and Chat Completions adapters. It asserts the outbound first request and linked result continuation for each of the five profiles before the native test previews, grants, applies, checks status, undoes/redoes, and checks deduplication. `p05-native-wire-events.jsonl` contains two complete 10-row runs because the suite was repeated: five initial and five continuation requests per run, all `outbound_assertions=passed`. These rows use dummy credentials and no network. They do not establish vendor server acceptance.

| Family | Focused service cases | Failure/completion/usage coverage |
|---|---|---|
| OpenAI Responses | `tests/provider.test.ts` | Fixed endpoint, serial tool schema, `call_id` result, complete response barrier, failed/incomplete turn, auth/429, cancellation, retry bounds, unknown reservation, cumulative usage and duplicate continuation. |
| Anthropic Messages | `tests/anthropic_messages.test.ts` (12 cases) | Message event/block assembly, tool-use/result linkage, thinking signature replay, cumulative usage, truncation, early EOF, late error, invalid/duplicate call, auth/429/503, cancellation. |
| Chat Completions | `tests/chat_completions.test.ts` (14 cases) | DeepSeek/Kimi reasoning, linked tool result, exact OpenRouter routing, split UTF-8/interleaved chunks, text-only/null, failed/unknown finish, multi-call/choice, HTTP-200 in-stream error, duplicate usage, cancellation/auth/429. |

The shared run manager tests cover immutable profile/model/route/scope/limits snapshots, one serial active run, retry accounting, stale/cancelled callbacks, proposal limits, unresolved native result blocking continuation, and unknown usage reservation. Native P05 cases cover unauthorized/route rejection, stale callback, profile-switch blocking during unresolved operation, and all five profile loops. An early `run_state` ordering issue and missing redirect guard caused earlier failures; the later passing code and test run did not erase `p05-native-wire-first-failed-manifest.txt` and `p05-native-wire-first-events.jsonl`. The original first 2D smoke failure is separately retained as `p05-baseline-smoke-2d-first.txt`.

No SDK helper is given an editor tool executor. Only a validated completed turn can emit a normalized `tool_call_ready`; the editor checks intent, revision, preview, grant, journal status, and deduplication before an effect. These fixtures test failure guards offline; a live provider stream remains `not_run`.
